local Rayfield = nil
local success, err = pcall(function()
    Rayfield = loadstring(game:HttpGet("https://sirius.menu/rayfield"))()
end)

if not success or not Rayfield then
    Rayfield = loadstring(game:HttpGet("https://raw.githubusercontent.com/SiriusSoftwareLTD/Rayfield/main/source.lua"))()
end

local Window = Rayfield:CreateWindow({
    Name = "รวมโดยปาล์ม นราวิชญ์",
    Icon = 0,
    LoadingTitle = "กำลังโหลด...",
    LoadingSubtitle = "by ปาล์มหัวดอ",
    Theme = "Default",
    DisableRayfieldPrompts = false,
    DisableBuildWarnings = false,
    ConfigurationSaving = { Enabled = false },
    Discord = { Enabled = false },
    KeySystem = false
})

-- ========================================================
-- TABS & SERVICES
-- ========================================================
local MainTab = Window:CreateTab("Main", 4483362458)
local TeleportTab = Window:CreateTab("Teleport", 4483362458)
local GameTab = Window:CreateTab("Game", 4483362458)
local ESPTab = Window:CreateTab("ESP & Hitbox", 4483362458)
local FPSTab = Window:CreateTab("FPS System", 4483362458)
local SpyTab = Window:CreateTab("SimpleSpy", 4483362458)
local SettingsTab = Window:CreateTab("Settings", 4483362458)

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UserInputService = game:GetService("UserInputService")
local CoreGui = game:GetService("CoreGui")

local LocalPlayer = Players.LocalPlayer
local Camera = workspace.CurrentCamera
local Mouse = LocalPlayer:GetMouse()

--------------------------------------------------------
-- GUI TOGGLE (กดปุ่ม LeftControl เพื่อเปิด-ปิด Rayfield UI)
--------------------------------------------------------
local guiVisible = true

UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == Enum.KeyCode.LeftControl then
        guiVisible = not guiVisible
        local rayfieldGui = CoreGui:FindFirstChild("Rayfield") or LocalPlayer:WaitForChild("PlayerGui"):FindFirstChild("Rayfield")
        if rayfieldGui then
            local mainFrame = rayfieldGui:FindFirstChild("Main", true) or rayfieldGui:FindFirstChild("MainFrame", true)
            if mainFrame then
                mainFrame.Visible = guiVisible
            end
        end
    end
end)

--------------------------------------------------------
-- CUSTOM EXECUTOR UI (หน้าต่างแยกต่างหาก พร้อมระบบ Save/Load)
--------------------------------------------------------
local selectedSaveSlot = "Slot_1.txt" -- ค่าเริ่มต้นของ Slot

local ExecGui = Instance.new("ScreenGui")
local ExecFrame = Instance.new("Frame")
local ExecTitle = Instance.new("TextLabel")
local ExecCloseBtn = Instance.new("TextButton")
local ExecTextBox = Instance.new("TextBox")
local ExecRunBtn = Instance.new("TextButton")
local ExecClearBtn = Instance.new("TextButton")
local ExecSaveBtn = Instance.new("TextButton")
local ExecLoadBtn = Instance.new("TextButton")

ExecGui.Name = "CustomExecutorUI"
ExecGui.Parent = CoreGui
ExecGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ExecGui.ResetOnSpawn = false
ExecGui.Enabled = false

ExecFrame.Name = "MainFrame"
ExecFrame.Parent = ExecGui
ExecFrame.Active = true
ExecFrame.Draggable = true -- ทำให้ลากขยับหน้าต่างได้
ExecFrame.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
ExecFrame.BorderColor3 = Color3.fromRGB(60, 60, 60)
ExecFrame.Position = UDim2.new(0.3, 0, 0.2, 0)
ExecFrame.Size = UDim2.new(0, 450, 0, 320)

ExecTitle.Name = "Title"
ExecTitle.Parent = ExecFrame
ExecTitle.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
ExecTitle.Size = UDim2.new(1, 0, 0, 30)
ExecTitle.Font = Enum.Font.SourceSansBold
ExecTitle.Text = "   Custom Lua Script Executor"
ExecTitle.TextColor3 = Color3.fromRGB(255, 255, 255)
ExecTitle.TextSize = 14.000
ExecTitle.TextXAlignment = Enum.TextXAlignment.Left

ExecCloseBtn.Name = "CloseButton"
ExecCloseBtn.Parent = ExecTitle
ExecCloseBtn.BackgroundColor3 = Color3.fromRGB(200, 50, 50)
ExecCloseBtn.BorderColor3 = Color3.fromRGB(0, 0, 0)
ExecCloseBtn.Position = UDim2.new(1, -30, 0, 5)
ExecCloseBtn.Size = UDim2.new(0, 22, 0, 20)
ExecCloseBtn.Font = Enum.Font.SourceSansBold
ExecCloseBtn.Text = "X"
ExecCloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ExecCloseBtn.TextSize = 14.000

ExecTextBox.Name = "ScriptInput"
ExecTextBox.Parent = ExecFrame
ExecTextBox.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
ExecTextBox.BorderColor3 = Color3.fromRGB(50, 50, 50)
ExecTextBox.Position = UDim2.new(0.03, 0, 0.12, 0)
ExecTextBox.Size = UDim2.new(0.94, 0, 0.62, 0)
ExecTextBox.ClearTextOnFocus = false
ExecTextBox.MultiLine = true
ExecTextBox.Font = Enum.Font.Code
ExecTextBox.PlaceholderText = "-- พิมพ์โค้ด Lua ที่นี่..."
ExecTextBox.Text = "-- พิมพ์โค้ด Lua ที่ต้องการรันที่นี่\nprint('Hello from Custom Executor UI!')"
ExecTextBox.TextColor3 = Color3.fromRGB(0, 255, 128)
ExecTextBox.TextSize = 14.000
ExecTextBox.TextXAlignment = Enum.TextXAlignment.Left
ExecTextBox.TextYAlignment = Enum.TextYAlignment.Top

-- ปุ่ม Run
ExecRunBtn.Name = "RunButton"
ExecRunBtn.Parent = ExecFrame
ExecRunBtn.BackgroundColor3 = Color3.fromRGB(0, 180, 80)
ExecRunBtn.BorderColor3 = Color3.fromRGB(0, 0, 0)
ExecRunBtn.Position = UDim2.new(0.03, 0, 0.77, 0)
ExecRunBtn.Size = UDim2.new(0.45, 0, 0.1, 0)
ExecRunBtn.Font = Enum.Font.SourceSansBold
ExecRunBtn.Text = "Execute (รันสคริปต์)"
ExecRunBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ExecRunBtn.TextSize = 14.000

-- ปุ่ม Clear
ExecClearBtn.Name = "ClearButton"
ExecClearBtn.Parent = ExecFrame
ExecClearBtn.BackgroundColor3 = Color3.fromRGB(180, 50, 50)
ExecClearBtn.BorderColor3 = Color3.fromRGB(0, 0, 0)
ExecClearBtn.Position = UDim2.new(0.52, 0, 0.77, 0)
ExecClearBtn.Size = UDim2.new(0.45, 0, 0.1, 0)
ExecClearBtn.Font = Enum.Font.SourceSansBold
ExecClearBtn.Text = "Clear (ล้างข้อความ)"
ExecClearBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ExecClearBtn.TextSize = 14.000

-- ปุ่ม Save
ExecSaveBtn.Name = "SaveButton"
ExecSaveBtn.Parent = ExecFrame
ExecSaveBtn.BackgroundColor3 = Color3.fromRGB(50, 120, 200)
ExecSaveBtn.BorderColor3 = Color3.fromRGB(0, 0, 0)
ExecSaveBtn.Position = UDim2.new(0.03, 0, 0.89, 0)
ExecSaveBtn.Size = UDim2.new(0.45, 0, 0.08, 0)
ExecSaveBtn.Font = Enum.Font.SourceSansBold
ExecSaveBtn.Text = "Save (เซฟลงสล็อตที่เลือก)"
ExecSaveBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ExecSaveBtn.TextSize = 14.000

-- ปุ่ม Load
ExecLoadBtn.Name = "LoadButton"
ExecLoadBtn.Parent = ExecFrame
ExecLoadBtn.BackgroundColor3 = Color3.fromRGB(200, 140, 0)
ExecLoadBtn.BorderColor3 = Color3.fromRGB(0, 0, 0)
ExecLoadBtn.Position = UDim2.new(0.52, 0, 0.89, 0)
ExecLoadBtn.Size = UDim2.new(0.45, 0, 0.08, 0)
ExecLoadBtn.Font = Enum.Font.SourceSansBold
ExecLoadBtn.Text = "Load (โหลดจากสล็อตที่เลือก)"
ExecLoadBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
ExecLoadBtn.TextSize = 14.000

-- ฟังก์ชันการทำงานของปุ่มใน Custom Executor UI
ExecCloseBtn.MouseButton1Click:Connect(function()
    ExecGui.Enabled = false
end)

ExecClearBtn.MouseButton1Click:Connect(function()
    ExecTextBox.Text = ""
end)

ExecSaveBtn.MouseButton1Click:Connect(function()
    if writefile then
        local fileName = "PalmExecutor_" .. selectedSaveSlot
        local success, err = pcall(function()
            writefile(fileName, ExecTextBox.Text)
        end)
        if success then
            Rayfield:Notify({Title = "Executor Save", Content = "บันทึกสคริปต์ลง " .. fileName .. " สำเร็จ!", Duration = 3})
        else
            Rayfield:Notify({Title = "Save Error", Content = tostring(err), Duration = 4})
        end
    else
        Rayfield:Notify({Title = "Executor Error", Content = "Executor ของคุณไม่รองรับการเขียนไฟล์ (writefile)", Duration = 4})
    end
end)

ExecLoadBtn.MouseButton1Click:Connect(function()
    local fileName = "PalmExecutor_" .. selectedSaveSlot
    if readfile and isfile and isfile(fileName) then
        local success, result = pcall(function()
            return readfile(fileName)
        end)
        if success then
            ExecTextBox.Text = result
            Rayfield:Notify({Title = "Executor Load", Content = "โหลดสคริปต์จาก " .. fileName .. " สำเร็จ!", Duration = 3})
        else
            Rayfield:Notify({Title = "Load Error", Content = tostring(result), Duration = 4})
        end
    else
        Rayfield:Notify({Title = "Load Error", Content = "ไม่พบไฟล์ใน " .. fileName .. " หรือ Executor ไม่รองรับ", Duration = 4})
    end
end)

ExecRunBtn.MouseButton1Click:Connect(function()
    local success, err = pcall(function()
        local func, loadErr = loadstring(ExecTextBox.Text)
        if func then
            func()
        else
            error(loadErr)
        end
    end)
    
    if success then
        Rayfield:Notify({Title = "Executor", Content = "รันสคริปต์จาก UI แยกสำเร็จ!", Duration = 3})
    else
        Rayfield:Notify({Title = "Executor Error", Content = tostring(err), Duration = 5})
        warn("Executor Error: " .. tostring(err))
    end
end)

--------------------------------------------------------
-- MAIN TAB (หน้าต่างหลัก & ช้อยส์สำหรับ Save/Load)
--------------------------------------------------------
MainTab:CreateSection("Custom Script Loader")

MainTab:CreateButton({
    Name = "Execute Rootleak Script",
    Callback = function()
        local success, err = pcall(function()
            loadstring(game:HttpGet("https://raw.githubusercontent.com/Rootleak/roblox/refs/heads/main/main.lua"))()
        end)
        if success then
            Rayfield:Notify({Title = "Success", Content = "Rootleak Script Executed!", Duration = 3})
        else
            Rayfield:Notify({Title = "Error", Content = tostring(err), Duration = 5})
            warn("Rootleak Error: " .. tostring(err))
        end
    end,
})

MainTab:CreateButton({
    Name = "Execute Epik Dance R6 Remake",
    Callback = function()
        local success, err = pcall(function()
            loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Epik-dance-r6-remake-64511"))()
        end)
        if success then
            Rayfield:Notify({Title = "Success", Content = "Epik Dance R6 Executed!", Duration = 3})
        else
            Rayfield:Notify({Title = "Error", Content = tostring(err), Duration = 5})
            warn("Epik Dance Error: " .. tostring(err))
        end
    end,
})

MainTab:CreateButton({
    Name = "Execute Current Angle (Reanimate)",
    Callback = function()
        local success, err = pcall(function()
            loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-CurrentAngle-V2-Full-axis-reanimate-43351"))()
        end)
        if success then
            Rayfield:Notify({Title = "Success", Content = "Current Angle Executed!", Duration = 3})
        else
            Rayfield:Notify({Title = "Error", Content = tostring(err), Duration = 5})
            warn("Current Angle Error: " .. tostring(err))
        end
    end,
})

MainTab:CreateButton({
    Name = "Execute Epik R6 Dances (After Current Angle)",
    Callback = function()
        local success, err = pcall(function()
            loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Epik-R6-Dances-Remake-85095"))()
        end)
        if success then
            Rayfield:Notify({Title = "Success", Content = "Epik R6 Dances Executed!", Duration = 3})
        else
            Rayfield:Notify({Title = "Error", Content = tostring(err), Duration = 5})
            warn("Epik R6 Dances Error: " .. tostring(err))
        end
    end,
})

MainTab:CreateSection("Executor UI System")

MainTab:CreateButton({
    Name = "Open Custom Executor UI",
    Callback = function()
        ExecGui.Enabled = not ExecGui.Enabled
        Rayfield:Notify({Title = "Executor", Content = "เปิด/ปิด หน้าต่าง Executor UI แยกแล้ว!", Duration = 2})
    end,
})

-- สร้างช้อยส์ (Dropdown) สำหรับเลือกช่องบันทึกสคริปต์
MainTab:CreateDropdown({
    Name = "เลือกช่องเซฟสคริปต์ (Save Slots)",
    Options = {"Slot 1", "Slot 2", "Slot 3", "Slot 4", "Slot 5", "Slot 6"},
    CurrentOption = {"Slot 1"},
    MultipleOptions = false,
    Flag = "ExecutorSaveSlotDrop",
    Callback = function(Option)
        -- ดึงค่าที่เลือก และเปลี่ยนช่องว่างเป็น _ แล้วเติม .txt ต่อท้าย
        selectedSaveSlot = string.gsub(Option[1], " ", "_") .. ".txt"
        Rayfield:Notify({Title = "Slot Changed", Content = "เลือก " .. Option[1] .. " เป็นช่องเซฟ/โหลดปัจจุบันแล้ว!", Duration = 2})
    end,
})

MainTab:CreateSection("Movement & Exploits (Fly & Fling)")

-- Fly V3 Variables & Logic
local flying = false
local flySpeed = 50
local flyConnection = nil
local bv, bg

local function startFly()
    local char = LocalPlayer.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end
    local hrp = char.HumanoidRootPart
    local humanoid = char:FindFirstChildOfClass("Humanoid")
    if not humanoid then return end

    flying = true
    humanoid.PlatformStand = true

    bv = Instance.new("BodyVelocity")
    bv.Parent = hrp
    bv.MaxForce = Vector3.new(math.huge, math.huge, math.huge)
    bv.Velocity = Vector3.new(0, 0, 0)

    bg = Instance.new("BodyGyro")
    bg.Parent = hrp
    bg.MaxTorque = Vector3.new(math.huge, math.huge, math.huge)
    bg.CFrame = hrp.CFrame

    flyConnection = RunService.RenderStepped:Connect(function()
        if not flying or not char or not hrp.Parent then return end
        
        local camCFrame = Camera.CFrame
        local moveDir = Vector3.new(0, 0, 0)

        if UserInputService:IsKeyDown(Enum.KeyCode.W) then
            moveDir = moveDir + camCFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.S) then
            moveDir = moveDir - camCFrame.LookVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.A) then
            moveDir = moveDir - camCFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.D) then
            moveDir = moveDir + camCFrame.RightVector
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.Space) then
            moveDir = moveDir + Vector3.new(0, 1, 0)
        end
        if UserInputService:IsKeyDown(Enum.KeyCode.LeftShift) then
            moveDir = moveDir - Vector3.new(0, 1, 0)
        end

        bv.Velocity = moveDir * flySpeed
        bg.CFrame = camCFrame
    end)
end

local function stopFly()
    flying = false
    if flyConnection then
        flyConnection:Disconnect()
        flyConnection = nil
    end
    if bv then bv:Destroy() end
    if bg then bg:Destroy() end

    local char = LocalPlayer.Character
    if char then
        local humanoid = char:FindFirstChildOfClass("Humanoid")
        if humanoid then
            humanoid.PlatformStand = false
        end
    end
end

MainTab:CreateToggle({
    Name = "Enable Fly V3",
    CurrentValue = false,
    Flag = "FlyToggleMain",
    Callback = function(Value)
        if Value then
            startFly()
            Rayfield:Notify({Title = "Fly V3", Content = "Fly Activated! (Use W, A, S, D, Space, Shift)", Duration = 3})
        else
            stopFly()
            Rayfield:Notify({Title = "Fly V3", Content = "Fly Deactivated!", Duration = 2})
        end
    end,
})

MainTab:CreateSlider({
    Name = "Fly Speed",
    Range = {16, 300},
    Increment = 5,
    Suffix = "Speed",
    CurrentValue = 50,
    Flag = "FlySpeedSliderMain",
    Callback = function(Value)
        flySpeed = Value
    end,
})

-- Player Fling Variables & Logic
local flinging = false
local flingConnection = nil

local function startFling()
    flinging = true
    flingConnection = RunService.Heartbeat:Connect(function()
        if not flinging then return end
        local char = LocalPlayer.Character
        if not char or not char:FindFirstChild("HumanoidRootPart") then return end
        local hrp = char.HumanoidRootPart
        
        local vel = hrp.Velocity
        hrp.Velocity = vel * 10000 + Vector3.new(0, 10000, 0)
        RunService.RenderStepped:Wait()
        if hrp and hrp.Parent then
            hrp.Velocity = vel
        end
    end)
end

local function stopFling()
    flinging = false
    if flingConnection then
        flingConnection:Disconnect()
        flingConnection = nil
    end
end

MainTab:CreateToggle({
    Name = "Enable Player Fling",
    CurrentValue = false,
    Flag = "FlingToggleMain",
    Callback = function(Value)
        if Value then
            startFling()
            Rayfield:Notify({Title = "Player Fling", Content = "Fling Activated! (Walk into players to fling them)", Duration = 3})
        else
            stopFling()
            Rayfield:Notify({Title = "Player Fling", Content = "Fling Deactivated!", Duration = 2})
        end
    end,
})

MainTab:CreateSection("Tool Giver Systems")

local GhostScreenGui = Instance.new("ScreenGui")
local GhostFrame = Instance.new("Frame")
local GhostScrollingFrame = Instance.new("ScrollingFrame")
local GhostUIListLayout = Instance.new("UIListLayout")
local GhostTextButton = Instance.new("TextButton")
local GhostTextLabel = Instance.new("TextLabel")
local GhostUpdateBtn = Instance.new("TextButton")

GhostScreenGui.Parent = CoreGui
GhostScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
GhostScreenGui.ResetOnSpawn = false
GhostScreenGui.Enabled = false 

GhostFrame.Parent = GhostScreenGui
GhostFrame.Active = true
GhostFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
GhostFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
GhostFrame.Position = UDim2.new(0.0610425249, 0, 0.0939490423, 0)
GhostFrame.Size = UDim2.new(0, 218, 0, 225)

GhostScrollingFrame.Parent = GhostFrame
GhostScrollingFrame.Active = true
GhostScrollingFrame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
GhostScrollingFrame.BorderColor3 = Color3.fromRGB(0, 0, 0)
GhostScrollingFrame.Position = UDim2.new(0.0871559605, 0, 0.155555561, 0)
GhostScrollingFrame.Size = UDim2.new(0, 187, 0, 136)
GhostScrollingFrame.CanvasSize = UDim2.new(0, 0, 35, 0)

GhostUIListLayout.Parent = GhostScrollingFrame
GhostUIListLayout.SortOrder = Enum.SortOrder.LayoutOrder

GhostTextButton.Parent = GhostScrollingFrame
GhostTextButton.BackgroundColor3 = Color3.fromRGB(117, 117, 117)
GhostTextButton.BorderColor3 = Color3.fromRGB(0, 0, 0)
GhostTextButton.Size = UDim2.new(0, 155, 0, 39)
GhostTextButton.Visible = false
GhostTextButton.Font = Enum.Font.SourceSans
GhostTextButton.TextColor3 = Color3.fromRGB(0, 0, 0)
GhostTextButton.TextSize = 20.000
GhostTextButton.TextStrokeColor3 = Color3.fromRGB(255, 255, 255)
GhostTextButton.TextStrokeTransparency = 0.000
GhostTextButton.TextWrapped = true

GhostTextLabel.Parent = GhostFrame
GhostTextLabel.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
GhostTextLabel.BorderColor3 = Color3.fromRGB(0, 0, 0)
GhostTextLabel.Position = UDim2.new(-0.00129664713, 0, -0.000140406293, 0)
GhostTextLabel.Size = UDim2.new(0, 218, 0, 25)
GhostTextLabel.Font = Enum.Font.SourceSans
GhostTextLabel.Text = "Tool Giver"
GhostTextLabel.TextColor3 = Color3.fromRGB(0, 0, 0)
GhostTextLabel.TextSize = 14.000

GhostUpdateBtn.Parent = GhostFrame
GhostUpdateBtn.BackgroundColor3 = Color3.fromRGB(0, 255, 30)
GhostUpdateBtn.BorderColor3 = Color3.fromRGB(0, 0, 0)
GhostUpdateBtn.Position = UDim2.new(0.0825688094, 0, 0.804444432, 0)
GhostUpdateBtn.Size = UDim2.new(0, 180, 0, 30)
GhostUpdateBtn.Font = Enum.Font.SourceSans
GhostUpdateBtn.Text = "update list"
GhostUpdateBtn.TextColor3 = Color3.fromRGB(0, 0, 0)
GhostUpdateBtn.TextSize = 14.000

local function updateGhostList()
    for _, v in pairs(GhostScrollingFrame:GetDescendants()) do
        if v:IsA("TextButton") and v ~= GhostTextButton then
            v:Destroy()
        end
    end
    
    local function cloneToBackpack(toolName)
        local clonedTool = toolName:Clone()
        clonedTool.Parent = LocalPlayer:WaitForChild("Backpack")
    end
    
    for _, v in pairs(game:GetDescendants()) do
        if v:IsA("Tool") and v.Parent.Parent ~= LocalPlayer then
            local clonebutton = GhostTextButton:Clone()
            clonebutton.Parent = GhostScrollingFrame
            clonebutton.Visible = true
            clonebutton.Text = v.Name
            clonebutton.MouseButton1Click:Connect(function()
                cloneToBackpack(v)
            end)
        end
    end
end

GhostUpdateBtn.MouseButton1Click:Connect(updateGhostList)

local dragToggle = nil
local dragStart = nil
local startPos = nil

GhostFrame.InputBegan:Connect(function(input)
    if (input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch) then 
        dragToggle = true
        dragStart = input.Position
        startPos = GhostFrame.Position
        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragToggle = false
            end
        end)
    end
end)

UserInputService.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        if dragToggle then
            local delta = input.Position - dragStart
            GhostFrame.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
        end
    end
end)

MainTab:CreateButton({
    Name = "Open Tool Giver UI",
    Callback = function()
        GhostScreenGui.Enabled = not GhostScreenGui.Enabled
        if GhostScreenGui.Enabled then
            updateGhostList()
        end
        Rayfield:Notify({Title = "Tool Giver", Content = "Toggled Tool Giver Window!", Duration = 2})
    end,
})

MainTab:CreateButton({
    Name = "Open Ghost Hub UI",
    Callback = function()
        GhostScreenGui.Enabled = not GhostScreenGui.Enabled
        if GhostScreenGui.Enabled then
            updateGhostList()
        end
        Rayfield:Notify({Title = "Ghost Hub", Content = "Toggled Ghost Hub Window!", Duration = 2})
    end,
})

MainTab:CreateSection("Admin & Scripts")

MainTab:CreateButton({
    Name = "Execute Infinite Yield",
    Callback = function()
        loadstring(game:HttpGet('https://raw.githubusercontent.com/EdgeIY/infiniteyield/master/source'))()
        Rayfield:Notify({Title = "Success", Content = "Infinite Yield Executed!", Duration = 3})
    end,
})

MainTab:CreateSection("Combat Tools")

MainTab:CreateButton({
    Name = "Execute Drop Kick Script",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/platinww/CrustyMain/refs/heads/main/universal/DropKick.lua"))()
        Rayfield:Notify({Title = "Success", Content = "Drop Kick Executed!", Duration = 3})
    end,
})

--------------------------------------------------------
-- TELEPORT SYSTEM TAB
--------------------------------------------------------
TeleportTab:CreateSection("Player Teleport")

local selectedPlayerName = ""

local function GetPlayerNames()
    local names = {}
    for _, p in pairs(Players:GetPlayers()) do
        if p ~= LocalPlayer then
            table.insert(names, p.Name)
        end
    end
    return names
end

local PlayerDropdown = TeleportTab:CreateDropdown({
    Name = "Select Player",
    Options = GetPlayerNames(),
    CurrentOption = {},
    MultipleOptions = false,
    Flag = "TeleportPlayerDropdown",
    Callback = function(Option)
        selectedPlayerName = Option[1] or ""
    end,
})

TeleportTab:CreateButton({
    Name = "Refresh Player List",
    Callback = function()
        PlayerDropdown:Set(GetPlayerNames())
    end,
})

TeleportTab:CreateButton({
    Name = "Teleport to Player",
    Callback = function()
        if selectedPlayerName ~= "" then
            local targetPlayer = Players:FindFirstChild(selectedPlayerName)
            if targetPlayer and targetPlayer.Character and targetPlayer.Character:FindFirstChild("HumanoidRootPart") then
                if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    LocalPlayer.Character.HumanoidRootPart.CFrame = targetPlayer.Character.HumanoidRootPart.CFrame * CFrame.new(0, 0, 3)
                    Rayfield:Notify({Title = "Teleport", Content = "Teleported to " .. selectedPlayerName, Duration = 2})
                end
            end
        else
            Rayfield:Notify({Title = "Error", Content = "Please select a player first!", Duration = 2})
        end
    end,
})

TeleportTab:CreateSection("Position Save & Load")

local savedCFrame = nil

TeleportTab:CreateButton({
    Name = "Save Current Position",
    Callback = function()
        if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
            savedCFrame = LocalPlayer.Character.HumanoidRootPart.CFrame
            Rayfield:Notify({Title = "Position Saved", Content = "Saved current location!", Duration = 3})
        end
    end,
})

TeleportTab:CreateButton({
    Name = "Teleport to Saved Position",
    Callback = function()
        if savedCFrame then
            if LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                LocalPlayer.Character.HumanoidRootPart.CFrame = savedCFrame
                Rayfield:Notify({Title = "Teleport", Content = "Teleported to saved location!", Duration = 2})
            end
        else
            Rayfield:Notify({Title = "Error", Content = "No position saved yet!", Duration = 2})
        end
    end,
})

--------------------------------------------------------
-- GAME TAB (สคริปต์แมพดัง ไม่มีคีย์ / No Key)
--------------------------------------------------------
GameTab:CreateSection("Popular Games Scripts (No Key)")

GameTab:CreateButton({
    Name = "Execute MM2 Script (Vynixius / No Key)",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/ILoGi/Main/main/MM2"))()
        Rayfield:Notify({Title = "Executed", Content = "Loaded Murder Mystery 2 Script!", Duration = 3})
    end,
})

GameTab:CreateButton({
    Name = "Execute BedWars Script (Vape V4 / No Key)",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/7GrandDadPGN/VapeV4ForRoblox/main/NewMainScript.lua", true))()
        Rayfield:Notify({Title = "Executed", Content = "Loaded Vape V4 (BedWars/Universal)!", Duration = 3})
    end,
})

GameTab:CreateButton({
    Name = "Execute Arsenal Script (ThunderClient / No Key)",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/ThunderTheMage/ThunderClient/main/Loader.lua"))()
        Rayfield:Notify({Title = "Executed", Content = "Loaded Arsenal Script!", Duration = 3})
    end,
})

GameTab:CreateButton({
    Name = "Execute Doors Script (FFJ Hub / No Key)",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/FFJHub/FFJHub/main/Doors/Doors.lua"))()
        Rayfield:Notify({Title = "Executed", Content = "Loaded Doors Script!", Duration = 3})
    end,
})

GameTab:CreateButton({
    Name = "Execute Blade Ball Script (Redz Hub / No Key)",
    Callback = function()
        loadstring(game:HttpGet("https://raw.githubusercontent.com/realredz/BladeBall/main/Source.lua"))()
        Rayfield:Notify({Title = "Executed", Content = "Loaded Blade Ball Script!", Duration = 3})
    end,
})

--------------------------------------------------------
-- ESP & HITBOX TAB
--------------------------------------------------------
ESPTab:CreateSection("Player ESP Controls")

local ESP_Enabled = false
local TeamCheck_Enabled = false
local ESP_Boxes = {}

local function RemoveESP(player)
    if ESP_Boxes[player] then
        if ESP_Boxes[player].Highlight then ESP_Boxes[player].Highlight:Destroy() end
        if ESP_Boxes[player].Billboard then ESP_Boxes[player].Billboard:Destroy() end
        ESP_Boxes[player] = nil
    end
end

local function ApplyESP(player)
    if player == LocalPlayer then return end

    local function CharacterAdded(character)
        RemoveESP(player)
        if not ESP_Enabled then return end

        if TeamCheck_Enabled and player.Team ~= nil and player.Team == LocalPlayer.Team then
            return
        end

        local hrp = character:WaitForChild("HumanoidRootPart", 5)
        local humanoid = character:WaitForChild("Humanoid", 5)

        if hrp and humanoid then
            local isSameTeam = (player.Team ~= nil and player.Team == LocalPlayer.Team)
            local boxColor = isSameTeam and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)

            local highlight = Instance.new("Highlight")
            highlight.Name = "ESPHighlight"
            highlight.Adornee = character
            highlight.FillColor = boxColor
            highlight.FillTransparency = 0.5
            highlight.OutlineColor = Color3.fromRGB(255, 255, 255)
            highlight.OutlineTransparency = 0
            highlight.Parent = character

            local billboard = Instance.new("BillboardGui")
            billboard.Name = "ESPName"
            billboard.Adornee = hrp
            billboard.Size = UDim2.new(0, 200, 0, 50)
            billboard.StudsOffset = Vector3.new(0, 3, 0)
            billboard.AlwaysOnTop = true

            local nameLabel = Instance.new("TextLabel")
            nameLabel.Parent = billboard
            nameLabel.Size = UDim2.new(1, 0, 1, 0)
            nameLabel.BackgroundTransparency = 1
            nameLabel.TextColor3 = boxColor
            nameLabel.TextStrokeTransparency = 0
            nameLabel.TextSize = 14
            nameLabel.Font = Enum.Font.SourceSansBold

            local function updateText()
                if humanoid and humanoid.Health > 0 then
                    nameLabel.Text = string.format("%s | HP: %d/%d", player.Name, math.floor(humanoid.Health), math.floor(humanoid.MaxHealth))
                else
                    nameLabel.Text = player.Name .. " [DEAD]"
                end
            end

            updateText()
            humanoid.HealthChanged:Connect(updateText)
            billboard.Parent = character

            ESP_Boxes[player] = {
                Highlight = highlight,
                Billboard = billboard
            }
        end
    end

    if player.Character then
        task.spawn(function() CharacterAdded(player.Character) end)
    end
    player.CharacterAdded:Connect(CharacterAdded)
end

local function RefreshESP()
    for player, _ in pairs(ESP_Boxes) do
        RemoveESP(player)
    end
    if ESP_Enabled then
        for _, player in ipairs(Players:GetPlayers()) do
            ApplyESP(player)
        end
    end
end

ESPTab:CreateToggle({
    Name = "Enable Player ESP",
    CurrentValue = false,
    Flag = "PlayerESPToggle",
    Callback = function(Value)
        ESP_Enabled = Value
        RefreshESP()
    end,
})

ESPTab:CreateToggle({
    Name = "Team Check (Hide Teammates)",
    CurrentValue = false,
    Flag = "TeamCheckToggle",
    Callback = function(Value)
        TeamCheck_Enabled = Value
        RefreshESP()
    end,
})

ESPTab:CreateSection("Hitbox Extender")

local HitboxEnabled = false
local HitboxSize = 2
local HitboxConnection = nil

local function ApplyHitbox()
    if not HitboxEnabled then return end
    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            local hrp = player.Character:FindFirstChild("HumanoidRootPart")
            if hrp then
                hrp.Size = Vector3.new(HitboxSize, HitboxSize, HitboxSize)
                hrp.Transparency = 0.7
                hrp.BrickColor = BrickColor.new("Really red")
                hrp.Material = Enum.Material.Neon
                hrp.CanCollide = false
            end
        end
    end
end

ESPTab:CreateToggle({
    Name = "Enable Hitbox Extender",
    CurrentValue = false,
    Flag = "HitboxToggle",
    Callback = function(Value)
        HitboxEnabled = Value
        if HitboxEnabled then
            HitboxConnection = RunService.RenderStepped:Connect(ApplyHitbox)
            Rayfield:Notify({Title = "Hitbox", Content = "Hitbox Extender Activated!", Duration = 2})
        else
            if HitboxConnection then
                HitboxConnection:Disconnect()
                HitboxConnection = nil
            end
            for _, player in pairs(Players:GetPlayers()) do
                if player ~= LocalPlayer and player.Character then
                    local hrp = player.Character:FindFirstChild("HumanoidRootPart")
                    if hrp then
                        hrp.Size = Vector3.new(2, 2, 1)
                        hrp.Transparency = 1
                        hrp.CanCollide = false
                    end
                end
            end
            Rayfield:Notify({Title = "Hitbox", Content = "Hitbox Extender Deactivated!", Duration = 2})
        end
    end,
})

ESPTab:CreateSlider({
    Name = "Hitbox Size",
    Range = {2, 25},
    Increment = 1,
    Suffix = "Studs",
    CurrentValue = 2,
    Flag = "HitboxSlider",
    Callback = function(Value)
        HitboxSize = Value
    end,
})

Players.PlayerAdded:Connect(function(player)
    player.CharacterAdded:Connect(function(char)
        if ESP_Enabled then
            task.wait(1)
            ApplyESP(player)
        end
    end)
end)

--------------------------------------------------------
-- FPS TAB (AIMBOT & FOV)
--------------------------------------------------------
FPSTab:CreateSection("Targeting System (Aimbot)")

local LockOnEnabled = false
local ShowFOVCircle = false
local AimFOVSize = 150
local LockConnection = nil

local FOVCircle = nil
pcall(function()
    FOVCircle = Drawing.new("Circle")
    FOVCircle.Visible = false
    FOVCircle.Transparency = 0.7
    FOVCircle.Color = Color3.fromRGB(255, 255, 255)
    FOVCircle.Thickness = 1
    FOVCircle.Filled = false
end)

local function GetClosestPlayerInFOV()
    local closestPart = nil
    local shortestDistance = AimFOVSize

    if not LocalPlayer.Character or not LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
        return nil
    end

    for _, player in pairs(Players:GetPlayers()) do
        if player ~= LocalPlayer and player.Character then
            if not (player.Team ~= nil and player.Team == LocalPlayer.Team) then
                local char = player.Character
                local humanoid = char:FindFirstChildOfClass("Humanoid")
                local targetPart = char:FindFirstChild("Head") or char:FindFirstChild("HumanoidRootPart")

                if humanoid and humanoid.Health > 0 and targetPart then
                    local screenPoint, onScreen = Camera:WorldToViewportPoint(targetPart.Position)
                    if onScreen then
                        local mousePos = Vector2.new(Mouse.X, Mouse.Y)
                        local distance = (Vector2.new(screenPoint.X, screenPoint.Y) - mousePos).Magnitude
                        if distance < shortestDistance then
                            shortestDistance = distance
                            closestPart = targetPart
                        end
                    end
                end
            end
        end
    end

    return closestPart
end

FPSTab:CreateToggle({
    Name = "Camera Target Lock",
    CurrentValue = false,
    Flag = "TargetLockToggle",
    Callback = function(Value)
        LockOnEnabled = Value

        if LockOnEnabled then
            LockConnection = RunService.RenderStepped:Connect(function()
                if FOVCircle then
                    FOVCircle.Visible = ShowFOVCircle
                    FOVCircle.Radius = AimFOVSize
                    FOVCircle.Position = Vector2.new(Mouse.X, Mouse.Y + 36)
                end

                local target = GetClosestPlayerInFOV()
                if target and LocalPlayer.Character and LocalPlayer.Character:FindFirstChild("HumanoidRootPart") then
                    local currentCFrame = Camera.CFrame
                    local targetCFrame = CFrame.new(Camera.CFrame.Position, target.Position)
                    Camera.CFrame = currentCFrame:Lerp(targetCFrame, 0.2)
                end
            end)
        else
            if FOVCircle then FOVCircle.Visible = false end
            if LockConnection then
                LockConnection:Disconnect()
                LockConnection = nil
            end
        end
    end,
})

FPSTab:CreateToggle({
    Name = "Show FOV Circle",
    CurrentValue = false,
    Flag = "FOVCircleToggle",
    Callback = function(Value)
        ShowFOVCircle = Value
        if FOVCircle then
            FOVCircle.Visible = Value
        end
    end,
})

FPSTab:CreateSlider({
    Name = "Aimbot FOV Size",
    Range = {50, 400},
    Increment = 5,
    Suffix = "Pixels",
    CurrentValue = 150,
    Flag = "AimFOVSlider",
    Callback = function(Value)
        AimFOVSize = Value
        if FOVCircle then
            FOVCircle.Radius = Value
        end
    end,
})

--------------------------------------------------------
-- SIMPLESPIV3 TAB
--------------------------------------------------------
SpyTab:CreateSection("SimpleSpy V3 Controls")

SpyTab:CreateButton({
    Name = "Execute SimpleSpy V3",
    Callback = function()
        task.spawn(function()
            local success, err = pcall(function()
                loadstring(game:HttpGet("https://raw.githubusercontent.com/78n/SimpleSpy/main/SimpleSpySource.lua"))()
            end)
            if not success then
                pcall(function()
                    loadstring(game:HttpGet("https://raw.githubusercontent.com/exxtremeshadow/SimpleSpySource/master/SimpleSpy.lua"))()
                end)
            end
            Rayfield:Notify({Title = "Success", Content = "SimpleSpy V3 Executed!", Duration = 3})
        end)
    end,
})
